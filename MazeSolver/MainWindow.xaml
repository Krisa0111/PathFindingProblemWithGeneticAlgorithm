<Window x:Class="MazeSolver.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Maze Solver" Height="450" Width="600">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Button Content="Solve Maze" Click="SolveMazeButton_Click" Margin="10" Grid.Row="0"/>
        <Button Content="Stop" Click="StopButton_Click" Margin="10" Grid.Row="1"/>
        <TextBlock x:Name="GenerationText" Grid.Row="2" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        <Canvas Name="MazeCanvas" Grid.Row="3"/>
    </Grid>
</Window>
